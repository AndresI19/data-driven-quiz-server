# Front-End — currently the delivery half of it: how static assets reach a browser, and why the
# obvious checks lie to you. Every card here is a failure this platform actually shipped: a garden
# image swapped in place that nobody could see for hours, and the 200 OK that hid a missing file.
#
# Authored like the Debugging deck: no card sets `recall: true`, and every card carries the fields
# (mc for identify, cloze for fill-in-the-blank) that let the mixed draw pick a machine-graded mode.
# It cannot be pinned to those — `fb`/`bf` are ungated in capabilities.ts, so open recall still comes
# up in the rotation. Which is why each `desc` below has to read on its own: identify HIDES the topic,
# so a desc that refers back to its own title ("the shortcut…") leaves nothing to reason from.
section:
  key: O
  name: Front-End
  color: "#84cc16"

cards:
  # ── Delivery & caching ────────────────────────────────────────────────────────────────────────
  - labels: [coding]
    topic: "Stale asset behind an unfingerprinted filename"
    desc: "New artwork is deployed over the old file at the same path, and the origin is confirmed to be returning the new bytes — yet visitors keep seeing the previous picture for hours, and a hard refresh changes nothing. The file ships from a static directory under a long max-age, so every browser and every CDN edge still considers the copy it already holds fresh, and nothing about the address changed to tell them otherwise."
    mc:
      - "The deploy rolled out but the pod is still running the old image"
      - "The file was written to the wrong static directory"
      - "The CDN is re-compressing the image and corrupting it"
      - "The browser has run out of room for its cache"
    cloze:
      text: "An asset replaced in place goes on serving stale because its {} never changed — so no cache has any reason to fetch it again."
      answer: "URL"
      alts:
        - "url"
        - "filename"
        - "file name"
        - "path"
        - "name"
    mcq:
      prompt: New artwork is deployed over the old file at the same path, the origin is confirmed to be serving the new bytes, yet visitors keep seeing the old picture for hours and a hard refresh makes no difference. Most likely cause?
      options:
      - The filename never changed
      - The pod runs the old image
      - Wrong static directory
      - CDN re-compression corrupted it
      answerIndex: 0

  - labels: [coding]
    topic: "Content fingerprinting"
    desc: "Build tools emit bundles under hashed names like index-CWr5tzLu.js so the file can be cached indefinitely, while any change to it ships under an address no cache has ever seen. Files copied verbatim out of a public/ or static/ directory never pass through that step — which is precisely why those are the assets that go stale after a swap."
    mc:
      - "Minification"
      - "Tree shaking"
      - "Code splitting"
      - "Source mapping"
    cloze:
      text: "Hashing a built file's name so that changing it ships a brand-new URL is called content {}."
      answer: "fingerprinting"
      alts:
        - "hashing"
        - "fingerprint"
    mcq:
      prompt: Build tools emit bundles under names like index-CWr5tzLu.js so that any change ships the file under an address no cache has seen. Which build step produces those names?
      options:
      - Minification
      - Tree shaking
      - Code splitting
      - Content hashing
      answerIndex: 3

  - labels: [coding]
    topic: "A hard refresh does not clear a CDN edge"
    desc: "Pressing Ctrl+Shift+R throws away the local browser copy and refetches, but the request still terminates at the nearest edge, which keeps serving the copy it holds for as long as that copy is fresh. Only an expiry, an explicit purge, or a changed address replaces it — which is also why a colleague routed to a different edge can honestly report seeing the new version while you still cannot."
    mc:
      - "It purges every cache between the client and the origin"
      - "It forces the edge to revalidate against the origin"
      - "Cache-Control applies only to browsers, never to a CDN"
      - "A private-browsing window bypasses the CDN"
    cloze:
      text: "A hard refresh clears only the {} cache — the CDN edge serves its own copy until it expires, is purged, or the URL changes."
      answer: "browser"
      alts:
        - "local"
        - "client"

  # ── Checks that lie to you ────────────────────────────────────────────────────────────────────
    mcq:
      prompt: An asset is stale for you but fresh for a colleague routed to a different edge, and a hard refresh does nothing. Which action actually replaces the copy that edge cache is serving?
      options:
      - A browser hard-refresh
      - A private-browsing window
      - A CDN purge
      - Clearing site cookies
      answerIndex: 2
  - labels: [coding]
    topic: "SPA catch-all masking a missing file"
    desc: "A single-page app's server ends its route table with a wildcard that returns index.html, so that deep links into client-side routes still work. A request for a file that does not exist therefore answers 200 with an HTML body instead of 404 — so a script that polls for the asset to appear reports success immediately, and a file that was never deployed looks like it arrived."
    mc:
      - "The CDN is rewriting 404 responses into 200s"
      - "The static directory was left out of the image"
      - "A service worker is intercepting the request"
      - "The health endpoint is masking the failure"
    cloze:
      text: "Behind an SPA wildcard route a 200 does not prove a file exists — read the {} to tell the fallback HTML apart from the asset you asked for."
      answer: "content-type"
      alts:
        - "content type"
        - "Content-Type"
        - "mime type"
        - "MIME type"
        - "media type"
    mcq:
      prompt: A deploy check polls an asset's URL to confirm it shipped and gets 200 OK — but the file was never built. Why does a request for a missing file return 200 instead of 404?
      options:
      - The wildcard serves index.html
      - The CDN rewrites 404s
      - A service worker intercepts
      - The static dir was omitted
      answerIndex: 0

  - labels: [coding]
    topic: 'loading="lazy" above the fold'
    desc: "Marking an image lazy defers its fetch until it nears the viewport, which buys exactly nothing for an image already in the viewport when the page arrives. A heavy hero or banner graphic stays in the critical path, and the attribute leaves a comfortable impression that page weight has been dealt with when it has not."
    mc:
      - "It defers the fetch until after the load event wherever the image sits"
      - "It lowers the request priority but still fetches immediately"
      - "It applies only once a service worker is registered"
      - "It has the browser re-encode the image at a smaller size"
    cloze:
      text: 'An image that is already {} the fold gains nothing from loading="lazy" — the attribute only defers fetches until the image nears the viewport.'
      answer: "above"
      alts:
        - "in view"
        - "visible"
        - "on screen"
        - "onscreen"
    mcq:
      prompt: A heavy hero banner sits in the viewport on load but paints slowly; its current loading="lazy" does nothing for it. Which loading-attribute value would instead tell the browser to fetch such a critical image right away?
      options:
      - lazy
      - eager
      - preload
      - prefetch
      answerIndex: 1
  - labels: [coding]
    topic: CORS preflight
    desc: A browser blocks a cross-origin POST carrying a custom header until the server answers an OPTIONS preflight request with the right Access-Control headers.
    cloze:
      text: A browser blocks a cross-origin POST carrying a custom header until the server answers an OPTIONS {} request with the right Access-Control headers.
      answer: preflight
    mcq:
      prompt: Your fetch to another origin fails in the browser with no response, and DevTools shows an OPTIONS call you never wrote. Which CORS mechanism is that automatic OPTIONS?
      options:
      - The preflight
      - A 301 redirect
      - A service worker
      - A socket upgrade
      answerIndex: 0
  - labels: [coding]
    topic: Forced reflow / layout thrash
    desc: Reading an element's offsetHeight and then writing a style in a tight loop forces the browser to recompute layout each iteration — a costly reflow.
    cloze:
      text: Reading an element's offsetHeight and then writing a style in a tight loop forces the browser to recompute layout each iteration — a costly {}.
      answer: reflow
    mcq:
      prompt: A scroll handler that measures then mutates DOM nodes one by one janks badly. Which browser cost is being triggered synchronously on every read-after-write?
      options:
      - Forced reflow
      - GPU compositing
      - JIT warmup
      - Garbage collection
      answerIndex: 0
  - labels: [coding]
    topic: Debounce vs throttle
    desc: Firing an autocomplete request only after the user stops typing for a moment, collapsing a burst into one call at the end, is debouncing.
    cloze:
      text: Firing an autocomplete request only after the user stops typing for a moment, collapsing a burst into one call at the end, is {}.
      answer: debouncing
    mcq:
      prompt: A resize handler runs hundreds of times per drag and you want it to run at most once every 100ms DURING the drag, not just when it ends. Which technique fits — as opposed to waiting for the burst to stop?
      options:
      - Throttling
      - Debouncing
      - Memoizing
      - Lazy-loading
      answerIndex: 0
  - labels: [coding]
    topic: Optimistic UI updates
    desc: Showing a 'liked' state instantly before the server confirms, then rolling back if the request fails, is an optimistic UI update.
    cloze:
      text: Showing a 'liked' state instantly before the server confirms, then rolling back if the request fails, is an {} UI update.
      answer: optimistic
    mcq:
      prompt: You want a comment to appear the instant a user hits send, without waiting on the round-trip, while still handling the rare failure. Which UI pattern is this?
      options:
      - Optimistic updates
      - Server-side rendering
      - Long polling
      - Skeleton loading
      answerIndex: 0
  - labels: [coding]
    topic: "Server-side rendering (SSR) vs. client-side rendering"
    desc: "Where the HTML is produced. With CSR the server sends a near-empty shell plus a JavaScript
      bundle, and nothing is visible until the browser has downloaded, parsed and executed it, then fetched
      data. With SSR the server renders real HTML for that request and sends complete markup, so the
      browser can paint immediately — then HYDRATION attaches event listeners to make it interactive."
    table:
      - - ""
        - CSR
        - SSR
      - - First paint
        - waits on the JS bundle
        - immediate, from HTML
      - - Server cost
        - serve static files
        - render per request
      - - Crawlers
        - see an empty shell
        - see the content
      - - Interactive
        - once JS runs
        - once hydration completes
    extras:
      - label: Say
        text: “SSR improves the metrics about SEEING content — first contentful paint and largest
          contentful paint — and can make time-to-first-byte WORSE, because the server now does work before
          responding. It is a trade, not a free win.”
      - label: Trap
        text: "hydration is the expensive part, and it creates an uncanny valley: the page looks ready and
          does not respond, because the markup arrived but the JavaScript that makes it interactive has not
          finished. Time-to-interactive can be later than it appears."
      - label: e.g.
        text: "the family is bigger than two. Static generation renders at BUILD time (fastest, but the
          content is fixed), SSR renders at REQUEST time (personalised, costs CPU per view), and
          incremental regeneration re-renders static pages on a schedule. Pick per route, not per app."
      - label: Optimisation
        text: personalised SSR is hard to cache — every response differs — so the usual pattern is to render
          the shared shell statically and stream or client-fetch only the per-user parts.
    cloze:
      text: After SSR delivers HTML, the browser must attach event listeners to it before the page becomes
        interactive — a step called {}.
      answer: hydration
    hint: Who builds the HTML, and what has to happen after it arrives.
    mcq:
      prompt: A team moves a marketing page from CSR to SSR. Which outcome should they expect?
      options:
        - Faster first contentful paint, but time-to-first-byte may worsen from per-request rendering
        - Faster time-to-first-byte, because less data is transferred
        - No change to crawler visibility, since crawlers execute JavaScript
        - Lower server cost, because rendering moves off the client
      answerIndex: 0
  - labels: [coding, security]
    topic: "SSR security: what leaks and what injects"
    desc: "Rendering on the server moves code, data and trust to a place the user does not control — which
      creates failure modes client-side rendering simply does not have. The two that matter most are the
      serialised state payload (what LEAKS) and server-side interpolation (what INJECTS)."
    items:
      - "State leakage — hydration needs the data the server rendered with, so it is embedded in the HTML
        as `__NEXT_DATA__` or `window.__INITIAL_STATE__`. EVERYTHING you put in server-side props ships to
        the browser: internal fields, other users' records, tokens. View source and it is right there."
      - "Script-breakout XSS — that serialised JSON sits inside a <script> tag, so an unescaped `</script>`
        inside user-supplied data closes the tag early and everything after it is executable markup."
      - "Template injection — frameworks escape interpolated values by default, but the escape hatches
        (dangerouslySetInnerHTML, v-html) do not, and on the server they run with server privileges."
      - "Cache poisoning — caching a rendered page that contains user-specific content serves one user's
        page to another. The cache key must include whatever the render varied on."
      - "SSRF — the server now fetches URLs while handling a user's request, so a user-influenced URL can
        reach internal services the browser never could."
    extras:
      - label: Say
        text: “The mental shift is that with SSR the render happens inside your trust boundary. Anything the
          renderer can reach — internal APIs, the metadata service, environment variables — is reachable
          while processing an untrusted request.”
      - label: Trap
        text: "environment variables are the quiet one. Bundlers inline any env var referenced in
          client-reachable code, so a secret read in a file that is imported by both server and client ends
          up in the shipped bundle. Prefix conventions exist precisely because this happens constantly."
      - label: Optimisation
        text: it is also a performance lever — trimming the serialised payload to only what hydration
          actually needs shrinks the HTML and speeds up parsing, so the security fix and the speed fix are
          the same change.
    cloze:
      text: The data a server-rendered page embeds in its HTML so the client can hydrate is the most common
        SSR {} — everything in server-side props is visible in view-source.
      answer: leak
      alts:
        - data leak
        - leakage
    hint: One risk is about what the page carries; the other is about what gets written into it.
    mcq:
      prompt: An SSR page passes a full user record into server-side props but renders only the display
        name. What is the security consequence?
      options:
        - The entire record is serialised into the HTML for hydration and is visible in view-source
        - Nothing, because only the rendered fields are sent
        - The record is exposed only if the hydration step fails
        - The fields are sent but encrypted with the session key
      answerIndex: 0
