# Keep the build context small and the image clean.
node_modules/
dist/
coverage/
.git/
.github/
.vite/

# The @platform/ui package lives inside the portfolio-home submodule. npm resolves
# `file:vendor/portfolio-home/packages/platform-ui` against it, so that path MUST be in the context —
# but nothing else in portfolio-home is needed here. Excluding the rest keeps the whole home page
# (its own src, its 1MB garden GIF, its résumé) out of the quiz's image.
vendor/portfolio-home/*
!vendor/portfolio-home/packages
vendor/portfolio-home/packages/*/node_modules/

# Not needed at runtime
*.md
!cards/*.md
.env
.env.*
*.log
.DS_Store
.idea/
.vscode/
